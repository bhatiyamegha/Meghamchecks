let count = 11;

const one = (sum=0, cb=() => {}) => {
    count = sum - 1;
    cb(count,cb);
};
one (count);
console.log(count);

const two = (sum=0, cb=() => {}) => {
    count = sum - 1;
    cb(count,cb);
};
two (count);
console.log(count);

const three = (sum=0, cb=() => {}) => {
    count = sum - 1;
    cb(count,cb);
};
three (count);
console.log(count);

const four = (sum=0, cb=() => {}) => {
    count = sum - 1;
    cb(count,cb);
};
four (count);
console.log(count);

const five = (sum=0, cb=() => {}) => {
    count = sum - 1;
    cb(count,cb);
};
five (count);
console.log(count);

const six = (sum=0, cb=() => {}) => {
    count = sum - 1;
    cb(count,cb);
};
six (count);
console.log(count);

const seven = (sum=0, cb=() => {}) => {
    count = sum - 1;
    cb(count,cb);
};
seven (count);
console.log(count);

const eight = (sum=0, cb=() => {}) => {
    count = sum - 1;
    cb(count,cb);
};
eight (count);
console.log(count);

const nine = (sum=0, cb=() => {}) => {
    count = sum - 1;
    cb(count,cb);
};
nine (count);
console.log(count);

const ten = (sum=0, cb=() => {}) => {
    count = sum - 1;
    cb(count,cb);
};
ten (count);
console.log(count);

console.log("Happy Independence Day")
